import java.util.ArrayList;
import java.util.UUID;

public class ProfileDetail extends DetailBasic {
    public ProfileDetail(UUID id, String name, ArrayList<String> values) {
        super(id, name, values);
    }
}
